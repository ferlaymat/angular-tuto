import { SelectStructuralDirective } from './select-structural-directive';

describe('SelectStructuralDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectStructuralDirective();
    expect(directive).toBeTruthy();
  });
});
